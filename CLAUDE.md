# CLAUDE.md — repo เว็บ ninethanakarn.com

**อ่านไฟล์นี้ให้จบก่อนแตะไฟล์ใดๆ ในโฟลเดอร์นี้**

เว็บนี้คือ "ต้นทางความจริง" ของเกณฑ์รับเคสที่ระบบบอทใช้ตอบลูกค้า และเป็นปลายทางเดียวที่ traffic จากโฆษณาไปถึง — แก้ผิดจุดเดียวพังทั้งสาย

- เจ้าของเนื้อหา/โครงสร้าง: session **WEB02**
- เจ้าของ tracking + ปุ่ม LINE: session **WEB01** (ห้ามแตะ ดูข้อ 1)
- เจ้าของฟอร์มประเมิน DSR: session **WEB03** (ยังไม่เปิด)
- ผลิตคอนเทนต์รายวัน: คำสั่ง `/content-pipeline` (local · คนละจังหวะกับ WEB02)
- ออกโดย BIZ04 · 1 ส.ค. 2569

---

## 1. ห้ามแตะ (ละเมิดแล้วต้องรื้อ)

1. **ปุ่ม LINE ทุกจุด และสคริปต์ tracking** — เป็นงาน WEB01 ที่ถูกล็อกไว้ว่าต้องผ่านเทส "เคส 4" ก่อน (บัญชีที่ยังไม่เป็นเพื่อนกับ OA กด `oaMessage` แล้วเปิดแชท+ส่งข้อความได้จริงไหม) แก้ก่อนอาจต้องรื้อสองรอบ

   สิ่งที่อยู่ในขอบเขตนี้:
   - ลิงก์ `line.me/R/oaMessage/...` — ปัจจุบัน **606 จุด ใน 73 ไฟล์**
   - event `click_line` — **73 จุด**
   - GA4 `gtag` (G-EKC3DMS6P5) · Meta Pixel `fbq` — ฝังครบทุกหน้าแล้ว
   - พารามิเตอร์ `gclid` (ยังไม่มีในโค้ดตอนนี้ — ถ้าเห็นเพิ่มมาแปลว่า WEB01 ลงมือแล้ว)

   > แก้ **ข้อความรอบๆ ปุ่ม** ได้ · แก้ **ตัวปุ่ม / href / onclick / event** ไม่ได้

2. **ห้าม merge branch `claude/business-forecast-5-years-mo3wdh` เข้า main** — repo นี้เป็น public แต่ branch นั้นเก็บเอกสารธุรกิจภายใน

3. **ห้ามแตะปุ่ม CTA ของเพจ Facebook ที่ชี้ LINE OA** (นอก repo แต่กฎเดียวกัน) — LINE conversion 39% เป็นช่องทางหลัก

4. **แก้ `llms.txt` หรือ FAQ เมื่อไหร่ ต้องแจ้ง BOT01 ทุกครั้ง** — สองอย่างนี้คือต้นทางเกณฑ์รับเคสที่บอทใช้ตอบลูกค้าจริง **แก้ข้อความเกณฑ์บนเว็บ = แก้สมองของ BOT01** นี่คือคู่ที่อันตรายที่สุดในระบบ

5. **ย้าย / เพิ่ม / ลบหน้า ต้องแก้ `sitemap.xml` ในคอมมิตเดียวกันเสมอ** ห้ามแยกทำทีหลัง

---

## 2. กติกาเนื้อหา (ใช้กับทุกข้อความที่ขึ้นเว็บ)

### คำ/ข้อความต้องห้าม

- ห้ามใช้คำว่า **"เท่านั้น"** · **"มีเงินทอน"** · **การันตีกู้ผ่าน**
- **ห้ามระบุชื่อธนาคารในเรื่องเล่าเคส** — ยกเว้น ธอส. ที่เปิดเผยได้
- ทุกหน้าที่พูดถึงการอนุมัติ ต้องระบุว่า **เป็นดุลยพินิจของธนาคาร ไม่รับรองผล**

### ตัวเลขที่อ้างอิงได้

- เพดานดอกเบี้ยตามเกณฑ์ ธปท.: **บัตรเครดิต 16%** · **บัตรกดเงินสด / P-loan 25%**
- **ห้ามใช้สูตร "ผ่อนล้านละ 6,000" กับผู้กู้อายุเกิน 45** — พิสูจน์แล้วว่าใช้กับอายุ 56 ไม่ได้ (วงเงินจริงเหลือครึ่งเดียว) บทความไหนเขียนสูตรนี้แบบไม่มีเงื่อนไขอายุ = ต้องแก้
- ห้ามแต่งตัวเลขราคา / ค่าเช่า / ดอกเบี้ย — ไม่มีข้อมูลให้เขียนว่าไม่มี

### The Mom Test (UI/UX)

- ข้อมูลสำคัญ **ห้ามซ่อนใต้ hover** — มือถือ hover ไม่ได้
- ปุ่ม Hard Commit (นัดคุย / ทักไลน์) ต้องเด่นชัด
- ต้องอ่านได้บนมือถือโดยไม่ต้อง interact

### กฎชั่วคราว — ถึง 21 ส.ค. 2569

**ห้ามใส่ URL ในแคปชั่น Instagram** (IG ถูกจำกัดแชร์ลิงก์ตั้งแต่ 22 ก.ค. · อุทธรณ์แล้ว) ใช้ "กดลิงก์ในโปรไฟล์" แทน

---

## 3. โครงไฟล์

static HTML ล้วน · **ไม่มี build step · ไม่มี framework · CSS/JS inline ในแต่ละไฟล์** แก้ไฟล์แล้ว push = ขึ้นเลย (GitHub Pages + CNAME)

```
/                       index.html — หน้าแรก
/about/                 เกี่ยวกับเรา · อ.ธรรศ
/debt/                  บริการปิดหนี้-รวมหนี้      ┐
/home/                  บ้านมือ 1                 │ 4 บริการหลัก
/condo/                 คอนโดลงทุน                │ (มี FAQ ฝังในหน้า)
/rent-to-own/           เช่าซื้อ / ผ่อนตรง         ┘
/home/<แบรนด์>/         หน้าแบรนด์บ้าน 20 ราย (ap, lh, spali, sena, ...)
/condo/<แบรนด์>/        หน้าแบรนด์คอนโด 20 ราย
/blog/                  content hub — บทความ 18 + การ์ดเคสจริง 3 ใบ + แท็บ
/blog/*.html            บทความ
/case/case-*.html       บทความเคสจริง 3 ไฟล์ (URL เดิม ยังใช้อยู่)
/loan/ /invest/         แอปคำนวณ (ทำงานในเครื่อง ไม่ส่งข้อมูลออก)
/saving/ /tax/
/privacy/ /terms/       หน้ากฎหมาย
404.html
llms.txt                ⚠️ ต้นทางเกณฑ์รับเคส — แก้แล้วแจ้ง BOT01
sitemap.xml             ⚠️ ต้องแก้พร้อมทุกครั้งที่ย้าย/เพิ่ม/ลบหน้า
robots.txt
```

### หน้า redirect (noindex — อย่าใส่ใน sitemap)

| จาก | ไป | เหตุผล |
|---|---|---|
| `/case/index.html` | `/blog/#cases` | ยุบเมนูเคสจริงเข้า blog hub 26 ก.ค. |
| `/rent/index.html` | `/rent-to-own/` | เปลี่ยนชื่อ path |

### ข้อเท็จจริงที่ต้องรู้

- **เว็บไม่มีฟอร์มส่งข้อมูลสักตัว** (ตรวจแล้ว 29 ก.ค. — หน้าที่มี `<input>` คือแอปคำนวณที่รันในเครื่อง) ทางเดียวที่คนเข้า funnel = **ปุ่ม LINE**
- บทความชุดเอกสาร 6 ตัว (`how-to-get-*`, `borrower-info-*`, `check-ghb-*`) คือของที่ BOT01 ส่งลิงก์ให้ลูกค้า — แก้แล้วต้องแจ้ง BOT01
- คิวคอนเทนต์และ `ARTICLE_HTML` อยู่ใน Notion
- ช่องทางลูกค้า: LINE `@ninethanakarn` · โทร `02-334-0940` (จ.–ส. 9:00–17:00)
- สำนักงาน: รังสิต · ศรีนครินทร์ · ศรีราชา

---

## 4. วิธี push

**push ได้ทางเดียว: local git ที่โฟลเดอร์นี้ (`Desktop\spare\ninethanakarn.com`)**
GitHub connector เป็น **read-only (403)** พิสูจน์แล้ว 22 ก.ค. 2569 — อย่าเสียเวลาลองซ้ำ

```bash
git add <ไฟล์ที่แก้>
git commit -m "<สรุปสิ่งที่เปลี่ยน + เหตุผล>"
git push origin main
```

- remote: `https://github.com/ninethanakarn/ninethanakarn.com.git` · branch หลัก `main`
- ⚠️ ตอนนี้มี `images/<แบรนด์>/` หลายโฟลเดอร์เป็น untracked — **`git add .` จะลากเข้าไปด้วย** ให้ระบุไฟล์ทีละตัวเสมอ
- commit message เขียนภาษาไทยได้ ระบุว่าเปลี่ยนอะไรและทำไม (ดูของเดิมใน `git log` เป็นตัวอย่าง)

---

## 5. เช็กลิสต์ก่อน commit

- [ ] ไม่ได้แตะปุ่ม LINE / `click_line` / `gtag` / `fbq`
- [ ] ถ้าย้าย-เพิ่ม-ลบหน้า → แก้ `sitemap.xml` แล้ว และไม่มีลิงก์เสีย
- [ ] ถ้าแก้ `llms.txt` หรือ FAQ ในหน้าใดก็ตาม → **แจ้ง BOT01**
- [ ] ไม่มีคำต้องห้าม ("เท่านั้น" / "มีเงินทอน" / การันตีกู้ผ่าน) และไม่มีชื่อธนาคารในเรื่องเล่าเคส (ยกเว้น ธอส.)
- [ ] ตัวเลขทุกตัวมีที่มา ไม่ได้เดา
- [ ] อ่านได้บนมือถือ · ข้อมูลสำคัญไม่ซ่อนใต้ hover
- [ ] `git add` ระบุไฟล์ ไม่ใช่ `.`

---

## 6. เกณฑ์วัดผล WEB02

1. **ความตรงของต้นทางความจริง** — เกณฑ์รับเคสบน FAQ / `llms.txt` ตรงกับที่ BOT01 ใช้จริง 100% ผิดข้อเดียวถือว่าไม่ผ่าน
2. **ไม่ทำของเดิมพัง** — ทุกครั้งที่ย้าย/แก้หน้า ต้องไม่มีลิงก์เสีย และ sitemap ต้องตรง
3. **งานค้างถูกเคลียร์** — ของที่ผลิตเสร็จแล้วไม่ควรค้าง deploy ข้ามสัปดาห์
4. **แจ้ง session ที่เกี่ยวข้องทุกครั้งที่แก้ของร่วม** (FAQ / `llms.txt` → BOT01) — pass/fail ไม่ใช่ %
